package org.example.lessons.lesson03.homework

class Homework {

    // 1. Название мероприятия (неизменяемая публичная переменная известная на момент написания)
        val eventName: String = "GG Hackathon"

    // 2. Дата проведения (публичная переменная известная на момент написания, но может быть изменена из-за переноса)
        val eventDate: String = "25 декабря 2026"

    // 3. Место проведения (публичная переменная известная на момент написания, но может быть изменена из-за переноса)
        val eventPlace: String = "Севастополь, ул.Пушкина, дом Колотушкина"

    // 4. Подробный бюджет мероприятия, включая расходы на оборудование, кейтеринг и другие операционные расходы.
    // (приватная неизменяемая переменная - её не должны увидеть и бюджет уже запланирован и утверждён и не должен изменяться)
        val detailedBudget: String = "1 миллион долларов, подробная информация с перечислением поставщиков и сумм для внутреннего пользования"

    //  5. Количество участников (публичная переменная известная на момент написания, но может быть изменена из-за ажиотажа или переноса)
        val participantCount: Int = 1

    //  6. Длительность хакатона
        val hackathonDuration: String = "8 часов"

    //  7. Контактная информация и условия соглашений с поставщиками пищи, оборудования и других услуг. (публичная переменная
    //  известная на момент написания, но может быть изменена из-за форс-мажора и смены поставщиков)
        val vendorAgreements: String = "контакты тех и других, также соглашения и список услуг"

    //  8. Текущее состояние хакатона (статус)  (публичная переменная известная на момент написания, но будет меняться при приближении мероприятия)
        val hackathonStatus: String = "Обработка заявок участников"

    //   9. Список спонсоров(приватная неизменяемая переменная - её не должны увидеть)
        private val sponsorsList: String = "Вася и Петя"

    //   10. Бюджет мероприятия (публичная переменная известная на момент написания, но может быть изменена из-за форс-мажора)
        val eventBudget: String = "3 рубля"

    //   11. Текущий уровень доступа к интернету (может поменяться)
        var internetAccessTier: String = "HIGH"

    //   12. Информация о транспортировке оборудования, распределении ресурсов и координации между различными командами поддержки. (супер приватно и изменяемо)
    private var logisticsPlan: String = "Быстрая доставка, годное распараделение ресурсов"
    //   13. Количество команд
        var teamCount: Int = 0
            set(value) {
                require(value >= 0) { "Количество команд не может быть отрицательным" }
                field = value
            }
    // 14. Перечень задач (публичный список, фиксированное содержимое)
    val tasks: List<String> = listOf(
        "Спроектировать архитектуру MVP",
        "Реализовать базовый REST API",
        "Собрать прототип интерфейса и провести юнит‑тесты"
    )

    // 15. План эвакуации (публичен, не изменяется)
    val evacuationPlan: String = "При ЧС — выход через главный и запасной выходы, сбор на площадке у парковки; ответственный — координатор смены"

    // 16. Список доступного оборудования (публичен, фиксированное содержимое)
    val availableEquipment: List<String> = listOf(
        "Ноутбуки x30",
        "Проекторы x4",
        "Маркерные доски x6",
        "Wi‑Fi роутеры x5",
        "Микрофоны на стойках x2"
    )

    // 17. Список свободного оборудования (публичен, изменяется; наружу — только чтение)
    private val _freeEquipment: MutableList<String> = mutableListOf("Проектор №2", "Доска №4", "Микрофон №1")
    val freeEquipment: List<String> get() = _freeEquipment.toList()

    // 18. График питания участников (публичен, значение неизвестно при старте)
    lateinit var cateringSchedule: String

    // 19. План мероприятий на случай сбоев (публичен, не изменяется)
    val contingencyPlan: String = "Сбой питания — запуск ИБП на 30 минут + переключение на генератор; сбой интернета — раздача LTE через 3 мобильные точки; отказ проектора — резервный проектор в кладовой"

    // 20. Список экспертов и жюри (публичен, содержимое может дополняться)
    val expertsAndJury: MutableList<String> = mutableListOf(
        "Елена Смирнова (CTO TechFlow)",
        "Дмитрий Волков (ведущий архитектор)",
        "Ольга Петрова (продуктовый менеджер)"
    )

    // 21. Политика конфиденциальности (публична, не изменяется)
    val privacyPolicyUrl: String = "https://docs.example.com/privacy-policy-2026"

    // 22. Приватные отзывы (внутренние, изменяются)
    private val privateFeedback: MutableList<String> = mutableListOf()

    // 23. Текущая температура в помещении (°C; публична, изменяется, с валидацией)
    var currentRoomTemperature: Double = 23.0
        private set(value) {
            require(value in 18.0..28.0) { "Температура $value выходит за комфортный диапазон 18–28°C" }
            field = value
        }

    // 24. Данные мониторинга сети (публичны, изменяются, обновляются системой)
    var networkMonitoringData: String = "Задержка: 18 мс, пропуск: 1.2 Гбит/с, потери: 0%"
        private set

    // 25. Регулятор скорости интернета (публичен, изменяется, валидация 0.0–1.0)
    var internetSpeedFactor: Double = 1.0
        set(value) {
            require(value in 0.0..1.0) { "Коэффициент должен быть в диапазоне 0.0..1.0" }
            field = value
        }

    // 26. Уровень освещения (в люксах; публичен, изменяется, валидация ≥0)
    var lightingLevelLux: Int = 450
        set(value) {
            require(value >= 0) { "Уровень освещения не может быть отрицательным" }
            field = value
        }

    // 27. Лог событий мероприятия (публичен для организаторов, только добавление)
    private val _eventLog: MutableList<String> = mutableListOf()
    val eventLog: List<String> get() = _eventLog.toList()
    fun logEvent(entry: String) { _eventLog.add(entry) }

    // 28. Возможность получения медицинской помощи (публична, не изменяется)
    val medicalAssistanceAvailable: Boolean = true

    // 29. Планы и процедуры безопасности (публичны, не изменяются)
    val safetyProcedures: String = "Охрана на входе, медпункт в зоне ресепшн, план эвакуации на каждом этаже, тревожная кнопка у администратора"

    // 30. Регистрационный номер мероприятия (публичен, не изменяется)
    val registrationNumber: String = "REG-2026-78901"

    // 31. Максимально допустимый уровень шума (дБ; публичен, не изменяется)
    val maxAllowedNoiseLevelDb: Int = 55

    // 32. Индикатор превышения уровня шума (публичен, изменяется, только чтение извне)
    var noiseLevelExceeded: Boolean = false
        private set

    // 33. План взаимодействия с прессой (публичен, не изменяется)
    val mediaInteractionPlan: String = "Брифинги в 11:00 и 17:00, пресс‑релиз по итогам, выделенный медиа‑координатор на площадке"

    // 34. Детальная публичная информация о проектах команд (публична, тяжёлая инициализация)
    val teamProjectsDetails: Map<String, String> by lazy {
        mapOf(
            "Team Alpha" to "Проект: чат‑бот для волонтёров — сборка OK, тесты 14/14, покрытие 82%",
            "Team Beta" to "Проект: трекер задач с AI — сборка OK, тесты 9/10, покрытие 67%",
            "Team Gamma" to "Проект: дашборд аналитики — сборка OK, тесты 12/12, покрытие 79%"
        )
    }

    // 35. Статус получения разрешений (публичен, изменяется, обновляется изнутри)
    var permitsState: String = "PENDING"

    // 36. Индикатор доступности специализированного оборудования (публичен, изменяется)
    var specializedEquipmentAvailable: Boolean = true

    // 37. Список партнёров мероприятия (публичен, содержимое может дополняться)
    val partners: MutableList<String> = mutableListOf(
        "TechFlow Inc.",
        "CloudNet Solutions",
        "DevTools Labs"
    )

    // 38. Отчёт (фото, видео, отзывы) — доступен после завершения (публичен, lateinit)
    lateinit var finalReportUrl: String

    // 39. Внутренние правила распределения призов (приватные, не изменяются)
    private val prizeDistributionRules: String = "Призы распределяются по решению жюри на основе баллов за функциональность, UX и инновационность; при равенстве — голосование всех экспертов"

    // 40. Список разыгрываемых призов (публичен, фиксирован)
    val prizes: List<String> = listOf(
        "Сертификат на обучение в школе разработки — 100 000 ₽",
        "Игровой ноутбук с RTX 4070",
        "Годовая подписка на профессиональные инструменты разработчика"
    )

    // 41. Контактная информация экстренных служб (приватна, не изменяется)
    private val emergencyContacts: String = "Скорая: 103, Пожарная: 101, Охрана площадки: +7 (999) 123-45-67"

    // 42. Особые условия для участников с ОВЗ (публичны, не изменяются)
    val accessibilityConditions: String = "Пандусы и лифты, выделенное место рядом с входом, материалы в PDF и озвучке, сопровождение волонтёров по запросу"

    // 43. Общее настроение участников (публично, снимок на момент вычисления, lazy)
    val overallMood: String by lazy { "POSITIVE" }

    // 44. Подробный план хакатона (публичен, lateinit, устанавливается перед стартом)
    lateinit var detailedSchedule: String

    // 45. Имя знаменитого специального гостя (публичен, lateinit)
    lateinit var specialGuestName: String

    // 46. Максимальное количество людей, которое может вместить площадка (публично, не изменяется)
    val maxVenueCapacity: Int = 250

    // 47. Количество часов, отведённое каждой команде для работы над проектом (публично, не изменяется)
    val teamWorkHours: Int = 36

}